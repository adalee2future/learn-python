import math

print math

signal_power = 9.0
noise_power = 10.0
ratio = signal_power / noise_power
decimals = 10 * math.log10(ratio)
print decimals

print math.log(10)

print math.pi

print math.sin(math.pi/2)

print math.sqrt(2)

print math.pow(2, 3)