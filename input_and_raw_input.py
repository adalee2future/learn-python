# raw_input, reads one line 
# from standard input and returns it as a string 
# (removing the trailing newline)
word = raw_input('Type a word') # for example we type hello
print word

# input
# assumes the input is a valid Python expression and returns the evaluated result to you
word = input()  # for example we type 1 + 1, and we get 2
print word   

  

